# All optional — the app runs fully in demo mode without them.
# Real values get pasted into Netlify (Site configuration → Environment
# variables) by Brian, never committed.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

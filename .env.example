# Supabase — create a NEW project per app (free tier is fine)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional — email via Resend (inert unset)
RESEND_API_KEY=
EMAIL_FROM=

# Optional — brand overrides (defaults in lib/brand.ts)
NEXT_PUBLIC_BRAND_NAME=
NEXT_PUBLIC_BRAND_DOMAIN=
